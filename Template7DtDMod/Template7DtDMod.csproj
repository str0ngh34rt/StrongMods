<Project Sdk="Microsoft.NET.Sdk">
  <Import Project="..\build\Mod.props" />
<!--#if (IsTemplate) -->
  <PropertyGroup>
    <!-- The template project itself is scaffolding, not a shippable mod, so it must never install into the game.
         `dotnet new` strips this block when generating a project, so generated mods deploy normally. -->
    <ModDeploy>false</ModDeploy>
  </PropertyGroup>
<!--#endif -->
  <Import Project="..\build\Mod.targets" />
</Project>

<Project Sdk="Microsoft.NET.Sdk">
  <Import Project="..\build\Mod.props" />
  <PropertyGroup>
    <!-- Loads late, after the mods whose chat commands it wraps. -->
    <ModLoadPrefix>ZZZZZZZZZZ_</ModLoadPrefix>
  </PropertyGroup>
  <Import Project="..\build\Mod.targets" />
</Project>
